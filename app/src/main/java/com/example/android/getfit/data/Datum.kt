package com.example.android.getfit.data

class Datum {
    class Card(
        val id: Int,
        val dayTime: String,
        val duration: String,
        val pushups: String,
        val squats: String
        )
}